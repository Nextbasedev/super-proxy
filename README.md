# Super Proxy

Open-source multi-provider AI model gateway.

> Local OSS extraction in progress. Not published.

## Quick start

```bash
cp .env.example .env
npm ci
npm run build
npm start
```

Health:

```bash
curl -sS http://127.0.0.1:8080/health
```

See `OSS-SCOPE.md` and `ARCHITECTURE.md` (added during OSS hardening).
